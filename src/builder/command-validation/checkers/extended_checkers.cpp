//HEADER

#include "validator.hpp"

bool check::part(CmdSpec &cmd, int idx) {
	(void)cmd;
	(void)idx;
	return (true);
}

bool check::kick(CmdSpec &cmd, int idx) {
	(void)cmd;
	(void)idx;
	return (true);
}
